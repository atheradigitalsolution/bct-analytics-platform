<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="mail_template_followup" model="mail.template">
        <field name="name">SPK: Pengingat Penagihan</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="subject">Pengingat pembayaran {{ object.name }}</field>
        <field name="email_from">{{ (object.company_id.email or user.email_formatted) }}</field>
        <field name="partner_to">{{ object.partner_id.id }}</field>
        <field name="body_html" type="html">
            <div style="margin:0px;padding:0px;font-size:13px;">
                <p>Yang terhormat <t t-out="object.partner_id.name or ''"/>,</p>
                <p>Kami ingin mengingatkan invoice <strong t-out="object.name or ''"/>
                   dengan jatuh tempo <t t-out="object.invoice_date_due or ''"/>
                   dan sisa <strong t-out="format_amount(object.amount_residual, object.currency_id)"/>.</p>
                <p>Bila pembayaran sudah dilakukan, mohon abaikan pesan ini dan kirimkan
                   buktinya agar kami perbarui catatan kami.</p>
                <p>Terima kasih.</p>
            </div>
        </field>
        <field name="lang">{{ object.partner_id.lang }}</field>
        <field name="auto_delete" eval="True"/>
    </record>
</odoo>
